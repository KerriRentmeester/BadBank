// Bad Bank GitHub - probly needs editing yet
function Balance(){
    return (
      <h1>Balance</h1>
    )
  }


//  {/* Display the current balance */}
//  Current Balance: ${props.balance}